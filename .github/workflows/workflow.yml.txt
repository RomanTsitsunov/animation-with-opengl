name: GitHub Actions

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - name: Build project
        uses: nicledomaS/cmake_build_action@v1.4